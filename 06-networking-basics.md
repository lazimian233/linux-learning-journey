# 网络（Networking）



**什么是网络**

网络就是让不同计算机之间能够互相通信，例如：

```
你的电脑
      │
      │
Internet
      │
      ▼
GitHub 服务器
```

当你执行：

```bash
git clone ...
```

实际上就是：

```
你的电脑
        │
        ▼
发送请求
        │
        ▼
GitHub
        │
        ▼
返回代码
```

**网络中真正传输的是什么**

不是文件，而是数据（Data），所有的东西都会变成二进制数据进行传输

**IP 地址（IP Address）**

每一台联网设备都需要一个唯一的网络地址，这就是IP 地址

例如：

```
192.168.1.10
```

或者：

```
8.8.8.8
```

它们都是IP 地址

IP就像一个家庭住址，有了它才能在网络中找到你的电脑

**端口（Port）**

如果把IP比作住址，那么Port就像房间号

IP负责找到哪台电脑，Port负责找到电脑里的哪个程序

例如：

一台电脑：

```
IP：

192.168.1.10
```

里面同时运行：

```
浏览器

微信

SSH

MySQL
```

如果只有IP，网络就不知道消息应该交给哪个程序

于是每个程序都有一个端口，例如：

```
22
SSH

80
HTTP（网页）

443
HTTPS（安全网页）

3306
MySQL
```

所以网络真正找到的是：IP+Port

例如192.168.1.10:22，意思是192.168.1.10上的SSH 服务

```text
Internet
      │
      ▼
IP（找到哪台电脑）
      │
      ▼
Port（找到哪个程序）
      │
      ▼
Server（处理请求）
```

**客户端和服务器**

Client（客户端）：主动发起请求的程序

例如：当浏览器访问某个网址时，浏览器就是客户端，因为是浏览器主动发送请求

Server（服务器）：等待请求并处理请求的程序

例如：网站服务器一直在等待，直到有人访问，它才开始工作

```text
Client
    │
    │ 请求（Request）
    ▼
Server
    │
    │ 响应（Response）
    ▼
Client
```

Client 和 Server 可以同时存在，例如：一台电脑一边浏览 GitHub，一边运行python -m http.server，所以它既是客户端又是服务器

**为什么我们访问浏览器时不用输入端口号**

例如，我们访问：

```text
https://www.python.org
```

没有写:443，但还能访问

因为浏览器知道，HTTPS 的默认端口就是443

所以实际上浏览器访问的是：

```text
https://www.python.org:443
```

只是443 可以省略

同样，HTTP的默认端口是80

所以：

```text
http://example.com
```

实际上就是：

```text
http://example.com:80
```

**为什么开发的时候总看到 127.0.0.1:8000**

经常会看到：

```text
127.0.0.1:8000
```

127.0.0.1 表示：本机（自己的电脑）

它是固定的，被称为Loopback Address（回环地址），作用是让计算机自己访问自己，不代表本机的真实IP

8000 表示：这个程序监听的是8000端口

所以127.0.0.1:8000就是：自己电脑上的某个服务，它监听的是8000端口

例如：

```bash
python -m http.server 8000
```

浏览器访问：

```text
http://127.0.0.1:8000
```

浏览器（Client）就会连接到本机运行的 HTTP Server

**为什么网站打不开**

假设要打开浏览器：

```
https://www.python.org
```

结果一直转圈，可能有很多原因

```text
浏览器
    │
    ├── 没有网络 ❌
    ├── DNS 出错 ❌
    ├── 找不到服务器 ❌
    ├── 服务器关机 ❌
    └── 网站程序崩溃 ❌
```

那么到底是哪一步出了问题？这就是 ping存在的意义

------

## ping：测试两台计算机之间能不能通信

例如：

```bash
ping 8.8.8.8
```

意思就是：

```
我的电脑
      │
      │ 你在吗？
      ▼
8.8.8.8
```

如果对方回复：

```
在。
```

那么说明至少网络是通的

执行：

```bash
ping 8.8.8.8
```

可能得到：

```text
64 bytes from 8.8.8.8:
icmp_seq=1
time=12 ms

64 bytes from 8.8.8.8:
icmp_seq=2
time=11 ms

64 bytes from 8.8.8.8:
icmp_seq=3
time=10 ms
```

为什么会有很多个结果呢？因为默认会一直ping，每一次ping都会返回一组结果

把一次结果拆开看：

```text
64 bytes from 8.8.8.8
```

8.8.8.8 回复了，说明网络是通的

```
time=12 ms
```

数据从本机传输到8.8.8.8，再从8.8.8.8传输回本机，总共花了12毫秒

这叫作：往返时间（Round Trip Time，RTT）

通常time越小越好，比如3ms说明距离近、网络快，而200ms说明可能服务器在国外或网络比较拥堵

------

**怎样让ping停下来**

Linux 默认会一直：

```
Ping

Ping

Ping

……
```

直到按下Ctrl+C，例如：

```
^C

3 packets transmitted

3 received
```

此时会返回一个总结性的结果

发送：3 ，收到：3，说明没有丢包

网络传输的数据不是一个整体，发送了一个东西，它可能会拆成很多个数据包（Packet）

由于网络拥堵、WiFi不稳定、路由器问题、服务器繁忙等问题，部分数据包可能会丢失

总之：time反映了一次通信的速度，而packet loss反映了整体的稳定性

**ping 域名**

除了：

```bash
ping 8.8.8.8
```

还可以：

```bash
ping www.python.org
```

由于这里ping的是域名而不是IP，所以Linux首先要把域名解析成IP，然后再ping

**ping 不通就说明网站不能访问吗**

ping不通不一定是网站坏了，因为有些服务器为了安全，会直接忽略 ping 请求，这时候ping的话可能就会一直超时

例如，浏览器访问的是：

```
HTTP（80）

HTTPS（443）
```

而ping使用的是另一种网络协议（ICMP）

服务器允许HTTP，但是拒绝ICMP（ping）

所以：ping 能通，说明网络基本可达；ping 不通，不一定说明网站不能访问

**HTTP和HTTPS**

- HTTP：HyperText Transfer Protocol（超文本传输协议）

- HTTPS：HTTP Secure（加密版 HTTP）


浏览器访问网页，不是直接：

```
电脑 ---- 网页
```

而是使用一种规则通信：

```
浏览器

↓

HTTP/HTTPS协议

↓

服务器
```

例如，访问：

```
https://www.python.org
```

实际上浏览器说：

```
你好服务器

我要首页
```

服务器返回：

```
这是网页HTML
```

HTTP规定了双方怎么请求、怎么回复

**ping 能帮我们排查什么**

假设一个网站打不开，第一步：

```bash
ping 8.8.8.8
```

如果通，说明电脑联网了

第二步：

```bash
ping www.python.org
```

如果不通，有可能是DNS出问题了

第三步：

浏览器访问：

```
https://www.python.org
```

如果打不开，可能是网站自己的服务坏了

所以ping只是排查网络问题的其中一步

**什么是DNS**

全称：Domain Name System（域名系统）

作用：把域名转换成IP地址

因为IP比较难记（一大串数字），所以平时使用的都是域名（比如google.com）

访问网站时，DNS可以把我们输入的域名转换成IP，有了IP地址，浏览器才能连接到那个网站的服务器

------

## curl：向服务器要数据

curl 是一个网络客户端，用来向服务器发送请求，并把服务器返回的数据打印出来

例如：

```bash
curl https://www.python.org
```

输出可能是：

```html
<!DOCTYPE html>
<html>
<head>
...
```

浏览器会把HTML解析成网页，而curl不会解析，只是原样输出

可以这样理解：浏览器 = curl + HTML/CSS/JavaScript 渲染器

## ssh：远程登录另一台计算机

Secure Shell

例如：

```bash
ssh user@192.168.1.100
```

发生的是：

```text
你的电脑
SSH Client
      │
      ▼
192.168.1.100
SSH Server
```

登录成功以后，再输入各种命令，实际上都是ssh服务器在执行，而不是自己的电脑在执行

这里的ssh服务器就是那台IP为192.168.1.100的电脑上的 SSH Server 程序

所以SSH 的本质就是：把远程服务器的 Shell 放到本机的终端里

看到的是：

```
user@server:~$
```

已经不是本机

SSH默认监听的端口是22，所以实际上：

```bash
ssh user@192.168.1.100
```

等于：

```
连接：

192.168.1.100:22
```

## scp：在两台计算机之间复制文件

Secure Copy

例如：

把本地的main.py复制到服务器：

```bash
scp main.py user@192.168.1.100:/home/user/
```

把服务器的log.txt下载到本机：

```bash
scp user@192.168.1.100:/home/user/log.txt .
```

这里的  .  表示当前目录

SSH和SCP都属于SSH协议

## wget：从网络下载文件

例如：

```bash
wget https://example.com/file.zip
```

发生：

```text
服务器

↓

file.zip

↓

保存到当前目录
```

curl 和 wget 的区别：

- curl偏向于获取数据，例如API、网页、JSON
- wget偏向于下载文件，例如python脚本、图片、zip压缩包

另外，现在curl也可以下载文件了：

```bash
curl -O
```

## ss -tuln：看看哪些程序正在监听网络

启动：

```bash
python -m http.server 8000
```

现在浏览器可以访问127.0.0.1:8000了

但Linux 怎么知道8000端口已经被占用了？

```bash
ss -tuln
```

可能得到：

```text
Netid State  Local Address:Port

tcp   LISTEN 0.0.0.0:22

tcp   LISTEN 0.0.0.0:8000
```

这里的22表示SSH，8000表示Python HTTP Server

LISTEN表示：某个程序正在监听这个端口，等待连接

以前最常见：

```bash
netstat -tuln
```

现在更推荐：

```bash
ss -tuln
```

因为它速度更快、功能更多

另外还有：

~~~bash
ss -tulpn
~~~

它可以把进程名也给显示出来，所以实际更常用它

解释：

```
t tcp

u udp

l listening

p process

n 不解析名字
```

------

**网络分层**

```text
应用层     （我要传什么？关心的是业务内容）
│
│  HTTP/HTTPS   →要网页
│  SSH    →要远程登陆
│  DNS    →要查地址
│
▼
传输层     （怎样可靠地传输？）
│
│  TCP    →负责：数据完整、顺序正确、丢包重传
│  UDP    →负责：快速发送
│
▼
网络层     （送到哪台机器？）
│
│  IP    →定位到目标地址
│
▼
链路层     （现实世界中到底怎样传？电脑-路由器-运营商设备-最终的服务器）
│
│  Ethernet   →以太网，网线连接
│  Wi-Fi      →无线网络
│
▼
物理层
```

```
层（Layer）
    ↓
规定职责

协议（Protocol）
    ↓
实现这个职责的规则
```

| 层     | 作用           | 协议                  |
| ------ | -------------- | --------------------- |
| 应用层 | 处理应用通信   | HTTP、HTTPS、SSH、DNS |
| 传输层 | 负责端到端传输 | TCP、UDP              |
| 网络层 | 寻找目标机器   | IP                    |
| 链路层 | 局域网传输     | Ethernet、Wi-Fi       |

另外，局域网传输靠的是MAC地址，它表示的是“哪个网卡”

所以现在有两个地址：

| 地址     | 作用                   |
| -------- | ---------------------- |
| IP 地址  | 找到目标网络中的机器   |
| MAC 地址 | 找到局域网中的具体网卡 |

~~~
┌─────────────────┐
│ 应用层           │
│ HTTP数据         │
├─────────────────┤
│ TCP             │
│ 端口             │
├─────────────────┤
│ IP              │
│ 目标IP           │
├─────────────────┤
│ 链路层           │
│ 目标MAC          │
└─────────────────┘
~~~

**TCP和UDP**

它们都是传输层协议

- TCP：Transmission Control Protocol（传输控制协议），最大的特点是可靠

什么叫可靠？我发给你的数据，你必须完整收到，而且顺序不能乱

它通过三次握手（通信前双方互相确认）、确认机制ACK（发送数据包后需要对方回复，这个回复就叫作ACK，如果没有收到回复的话就认为丢包了，会重新传输）、排序（根据编号对收到的数据进行排序，再交给应用程序）等机制来确保可靠

适用于比较重要的数据，但是速度会慢一些

- UDP：User Datagram Protocol（用户数据报协议），最大的特点是快速

不管那么多，直接发送

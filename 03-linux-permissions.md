# 权限（Permission）



**为什么 Linux 需要权限**

假设一台电脑有很多用户：

```text
电脑
├── 张三
├── 李四
└── 管理员
```

如果张三可以随便删除系统文件/修改别人的文件/安装危险软件，系统就不安全

所以 Linux 给每个文件规定：谁可以读？谁可以写？谁可以执行？

------

## ls -l：查看文件权限以及归属

查看目录内容，长格式列表，例如：

```bash
ls -l
```

输出：

```text
-rw-r--r-- 1 lazimian233 lazimian233 1200 hello.py
```

这一长串里最重要的是前面的 -rw-r--r--，我们拆开：

```text
- rw- r-- r--
│ │   │   │
│ │   │   └── 其他人权限
│ │   └────── 同组用户权限
│ └────────── 文件拥有者权限
└──────────── 文件类型
```

另外：这里有两个lazimian233，第一个是owner（拥有者），第二个是group（所属组）

再另外：输入ls -l ，查看的是目录下所有文件和文件夹（除了像 .venv 这样默认隐藏的）的详细信息，如果只想看目录下的文件夹的权限，不想看里面文件的权限，输入的是：

~~~bash
ls -ld 目录名（查看当前目录可省略）
~~~

------

**文件类型**

常见：

| 符号 | 含义     |
| ---- | -------- |
| -    | 普通文件 |
| d    | 目录     |
| l    | 链接     |

**三种权限**

Linux 权限只有三个：

对于文件而言：

- **r** —— read（读取，可以查看文件内容）
- **w** —— write（写入，可以修改文件内容）
- **x** —— execute（运行，可以运行文件）

对于目录而言：

- **r** —— read（可以查看目录里有哪些文件）
- **w** —— write（可以修改目录结构，如创建或删除文件、修改文件名等）
- **x** —— execute（可以进入目录；可以访问目录中的文件，前提是知道文件名）

按照“rwx”这个顺序写，缺少哪个权限就把这个权限对应的位置记为“-”

权限也可以用数字表示，r=4，w=2，x=1，例如：

rwx = 4+2+1 = 7

rw- = 4+2+0 = 6

r-x = 4+0+1 = 5

*【注】为什么有对文件的write权限，但却删除不了这个文件？*

*因为删除文件不是修改文件，实际上它修改的是文件的父目录（目录里保存着“文件名→文件位置”，删除文件就是从目录里移除这个文件名），所以能否删除文件取决于是否拥有对父目录的write权限*

**三类用户**

Linux 把用户分三类：

- owner（拥有者）：文件属于谁
- group（用户组）：组内的用户可以共享某些权限
- others（其他人）：除了拥有者和组成员之外的人

------

## chmod：修改权限

change mode（改变权限模式），例如：

现在：

```text
hello.sh
-rw-r--r--
```

没有执行权限，不能：

```bash
./hello.sh   （运行这个文件）
```

增加执行权限：

```bash
chmod +x hello.sh
```

这里的 +x 表示增加 execute 权限。此时权限变为：

```text
-rwxr-xr-x
```

数字方式修改权限，例如：

```bash
chmod 755 script.sh
```

表示：拥有者：rwx       同组用户和其他人：r-x

## sudo：以管理员身份执行

superuser do（以超级用户身份执行，超级用户也就是root），例如：

普通用户：

```bash
apt install python3
```

可能会输出：

```text
Permission denied
```

因为这里需要修改/usr 、/etc 、/bin这些系统目录，普通用户没有权限

但是：

```bash
sudo apt install python3
```

暂时借用 root 权限，所以就可以修改了

另外一个例子：

输入ls -l，输出：

~~~
root root config.txt
~~~

说明文件属于root，普通用户不能修改，此时就需要：

```bash
sudo
```

或者：

```bash
sudo chown
```

## chown：修改拥有者

change owner（改变拥有者），例如：

```bash
sudo chown root hello.py
```

就是把 hello.py 的拥有者改成 root
